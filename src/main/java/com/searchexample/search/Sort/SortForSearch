1.搜索的结果的排序算法：
    搜索结果的排序分值由两部分组成：
    一是搜索结果与输入关键字/词之间的关联强弱relationScore——可以使用某种基于TF与IDF的算法计算得到
    比如
    二是文档本身的权重k,比如令k(title) = 3 ;k(short_description) = 2 ;k(developer) = 1；

2.两部分之间应当是乘法关系，具体的权重需要测试以调整权重对于整体排序结果的影响。

